#!/usr/bin/env sh
repo_root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
log_file="$repo_root/.git/auto-push-deploy.log"

{
  printf '\n--- post-commit %s ---\n' "$(date '+%Y-%m-%d %H:%M:%S')"
  "$repo_root/scripts/auto_push_deploy.sh"
} >> "$log_file" 2>&1 &
